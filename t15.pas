program t15;
  // enum types
  
  type Direction = (North, East, South, West);
  var d1: Direction = East;

begin

  writeln( 'd1=', d1 );
  // writeln( ' ord=', ord(d1) );
  // d1 := pred(d1);
  // writeln( 'd1=', d1 );
  // writeln( ' ord=', ord(d1) );

end.
